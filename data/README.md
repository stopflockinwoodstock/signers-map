# Data Files

`city_overrides.csv` contains only aggregate city-level coordinate fallbacks for places that are not exact matches in `@countrystatecity/countries`.

It should stay small. Do not add signer rows, names, addresses, postal codes, emails, phone numbers, or signature dates.

Current overrides were verified as missing by exact city/state lookup in `@countrystatecity/countries`.
